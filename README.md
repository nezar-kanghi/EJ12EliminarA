En este ejercicio utilizamos el hashSet para almacenar varios nombres y elimina aquillos que comienzan por la letra "A". Primero añado varios nombres al conjunto y muestra sui contenido antes de la eliminarlo. luego alr ecorrer el set utilizando el iterator siempre que haya elementos comprobamos usando StartWith("A") si el nombre empieza por esa letra. si se da el caso se borra con it.remove().
